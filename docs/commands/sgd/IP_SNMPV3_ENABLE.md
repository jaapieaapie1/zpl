# ip.snmpv3.enable



This command returns the status of SNMPv3. If it is enabled, the command returns `"on"` . If SNMPv3 is
disabled, the command returns `"off"` .


**Getvar**


To view if SNMPv3 status is enabled:

```
       ! U1 getvar "ip.snmpv3.enable"

```

1320




SGD Network Commands